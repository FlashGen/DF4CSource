package com.developingcomponents.components		import mx.flash.UIMovieClip;		public class ErrorComponent extends UIMovieClip 	{		private static const MINIMUM_WIDTH	:Number = 200;		private static const MINIMUM_HEIGHT	:Number = 200;				private static const ERROR_VIEW		:String = "error";		private static const DEFAULT_VIEW	:String = "default";		public function ErrorComponent() 		{		}				override public function setActualSize(newWidth:Number, newHeight:Number):void		{			var _error	:Boolean = false;						if (newWidth != _width || newHeight != _height) 			{				_width = newWidth; 				_height = newHeight;								if(newWidth < MINIMUM_WIDTH)				{					_width = MINIMUM_WIDTH;					_error = true;				}								if(newHeight < MINIMUM_HEIGHT)				{					_height = MINIMUM_HEIGHT;					_error = true;				}			}						currentState = (_error)? ERROR_VIEW : DEFAULT_VIEW;		}	}}